import { WebMCPProbe } from "@/components/webmcp-probe";

/** Isolated native-client measurement surface; never used as product evidence itself. */
export default function WebMCPProbePage() {
  return <WebMCPProbe />;
}
